# Python 3.12 compatible runtime dependencies for Jo-SNC experiments.
#
# PyTorch is required by the project, but torch/torchvision should be installed
# separately with the CUDA wheel that matches the target machine.
# Recommended on NVIDIA H200 / Hopper:
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
# Install PyTorch first, then run:
#   pip install -r requirements.txt
# If the machine image provides an older CUDA 12 driver stack, use the matching
# official PyTorch cu126/cu124 wheel instead of mixing CUDA wheels.
#
# For reproducing the original paper environment on older CUDA/Python stacks,
# README.md records torch==1.12.1 and torchvision==0.13.1.

# config / logging
easydict==1.10
PyYAML>=6.0.1,<7
tqdm>=4.66,<5

# numeric / metrics
matplotlib>=3.8,<4
numpy>=1.26,<3
scikit-learn>=1.4,<2
torchmetrics>=1.3,<2

# vision / augmentation
Pillow>=10,<12
randaugment==1.0.2
timm>=1.0,<2
